import { PagePlaceholder } from "@/shared/components/PagePlaceholder";

export function ReportsPage() {
  return (
    <PagePlaceholder
      title="Reports"
      description="Analyze project progress, task completion rate, workload by member, overdue tasks, and workspace productivity."
    />
  );
}
