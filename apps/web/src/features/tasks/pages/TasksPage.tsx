import { PagePlaceholder } from "@/shared/components/PagePlaceholder";

export function TasksPage() {
  return (
    <PagePlaceholder
      title="Tasks"
      description="Create, assign, update, filter, and track tasks by project, status, priority, due date, and assignee."
    />
  );
}
