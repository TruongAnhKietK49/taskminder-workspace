import { PagePlaceholder } from "@/shared/components/PagePlaceholder";

export function WorkspacesPage() {
  return (
    <PagePlaceholder
      title="Workspaces"
      description="Manage workspaces that users own or have been invited to. This module will handle workspace members, roles, and invitations."
    />
  );
}
