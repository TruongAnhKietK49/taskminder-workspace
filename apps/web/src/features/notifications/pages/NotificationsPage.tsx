import { PagePlaceholder } from "@/shared/components/PagePlaceholder";

export function NotificationsPage() {
  return (
    <PagePlaceholder
      title="Notifications"
      description="Display system notifications, reminders, mentions, task changes, project updates, and invitation events."
    />
  );
}
