import { PagePlaceholder } from "@/shared/components/PagePlaceholder";

export function MembersPage() {
  return (
    <PagePlaceholder
      title="Members"
      description="Manage workspace and project members, roles, invitations, member activity, and access control."
    />
  );
}
