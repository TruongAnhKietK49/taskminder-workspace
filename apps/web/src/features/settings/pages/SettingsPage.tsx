import { PagePlaceholder } from "@/shared/components/PagePlaceholder";

export function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Configure account settings, notification preferences, workspace settings, privacy, and system behavior."
    />
  );
}
