import { PagePlaceholder } from "@/shared/components/PagePlaceholder";

export function ChatPage() {
  return (
    <PagePlaceholder
      title="Chat"
      description="Realtime conversations between workspace members, including project channels, direct messages, typing state, and presence."
    />
  );
}
