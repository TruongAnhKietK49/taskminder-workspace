import { PagePlaceholder } from "@/shared/components/PagePlaceholder";

export function ProjectsPage() {
  return (
    <PagePlaceholder
      title="Projects"
      description="Manage projects inside workspaces, including project members, project progress, task grouping, and project-level permissions."
    />
  );
}
